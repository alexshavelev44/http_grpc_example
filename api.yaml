type: google.api.Service
config_version: 3

http:
  rules:
    - selector: http_grpc_example.Bootcamp.GetAllMembers
      get: /members

    - selector: http_grpc_example.Bootcamp.CreateMember
      post: /members
      body: "*"
